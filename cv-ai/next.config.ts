import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  allowedDevOrigins: ["sloppily-stimulate-staunch.ngrok-free.dev"],

  serverExternalPackages: ["pdf-parse"],
};

export default nextConfig;
